<resources>

    <style name="globalTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@color/transparent</item>
    </style>

    <style name="listActivityTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="toolbar_homepage_title_text_appearance" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:textSize">27sp</item>
        <item name="android:textColor">@color/light_gray</item>
        <item name="android:letterSpacing">0.05</item>
    </style>


    <style name="collapsing_toolbar_title_text_expanded" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:textSize">36sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:letterSpacing">0.05</item>
    </style>

    <style name="collapsing_toolbar_title_text_collapsed" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:textSize">27sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:letterSpacing">0.05</item>
    </style>

    <!-- Inventory styles -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>

<!--        <item name="colorPrimaryDark">@color/black</item>-->
<!--        <item name="colorAccent">@color/black</item>-->
<!--        <item name="android:actionBar">@color/black</item>-->

        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="details_title">
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">8dp</item>
    </style>
    <!-- Inventory styles -->

</resources>